# databricks-hackathon-2025-babygpt

You need UV to run this project.

To run, navigate to the backend directory.

For Streamlit mode
`uv run streamlit run streamlit_app.py`


For CLI mode - 
`uv run python main.py --cli`